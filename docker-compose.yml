services:
  backend:
    build:
      context: .
      dockerfile: services/api/Dockerfile
    container_name: lagdoki_backend
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - whisper_cache:/root/.cache/huggingface
    restart: unless-stopped

volumes:
  whisper_cache: